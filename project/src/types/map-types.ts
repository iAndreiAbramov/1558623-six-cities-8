export type City = {
  location: {
    latitude: number,
    longitude: number,
    zoom: number,
  },
  name: string,
}

export type Point = {
  latitude: number,
  longitude: number,
  offerId: string,
}
